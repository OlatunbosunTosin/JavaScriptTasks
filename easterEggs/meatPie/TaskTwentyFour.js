const prompt = require("prompt-sync")();
let firstNum = Number(prompt("Enter first number: "));
let secondNum = Number(prompt("Enter second number: "));
let newFirstNum = secondNum;
let newSecondNum = firstNum;
console.log("First number is now %d and Second number is now %d", newFirstNum, newSecondNum);
   
